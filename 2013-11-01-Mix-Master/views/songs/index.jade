extends ../template/layout

block content
  .row
    .small-12.columns
  .row
    .small-12.columns
      #songs
        each song in songs
          .song
            a(href='/songs/#{song.id}')
              img(src=song.art)
            h6.subheader= song.title
